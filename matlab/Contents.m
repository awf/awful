% The awf utilities library.
%
% au_assert  Assert all(EXPR), print expr if not
% au_assert_equal  Assert all(EXPR1 == EXPR2), print expr if not
%
% au_whist  -  Fast weighted histogram
% au_sparse -  Fast construction of sparse matrices
